# Production requirements for Render deployment.
# Excludes sentence-transformers (torch ~1.5 GB) and deepeval (not needed at runtime).
# Uses OpenAI embeddings exclusively; reranker disabled via DISABLE_RERANKER=true.

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Config / env ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- LLM / agent stack ---
langchain==0.3.7
langchain-core==0.3.15
langchain-openai==0.2.5
langchain-community==0.3.5
langchain-text-splitters==0.3.2
langgraph==0.2.45
langsmith==0.1.142

# --- Embeddings / vector / retrieval ---
openai==1.54.3
chromadb==0.5.18
rank-bm25==0.2.2

# --- Data + ML ---
pandas>=2.2.3
numpy>=1.26.4
scikit-learn>=1.4
scipy>=1.11

# --- HTTP client (Groq fallback) ---
httpx==0.27.2
groq==0.11.0

# --- Scheduling ---
apscheduler==3.10.4

# --- Utils + document loaders ---
loguru==0.7.2
pypdf>=5.0
python-docx>=1.1.2
openpyxl>=3.1.5
beautifulsoup4>=4.12.3
lxml>=5.2.0
html2text>=2024.2.26
